// Escribir las siguientes funciones sobre fechas (puede resultar necesario escribir
// funciones auxiliares, como por ejemplo díaEnMesSiguienteAl_Del_, que reciba
// un día y un mes y retorne el siguiente día; ¿por qué es necesario saber el mes?):
// a. es_DelPrimerSemestre, que indica si el día determinado por la fecha
// dada es uno del primer semestre.
// b. es_DíaDeÑoquis, que indica si el día dado es un 29.
// c. es_DeAñoBisiesto, que dada una fecha, indica si la misma cae dentro de
// un año bisiesto.
// AYUDA: para saber si un año es bisiesto, hay que verificar que el año sea múltiplo de 4, pero
// no de 100 a menos que sea múltiplo de 400. Por ejemplo, 1796, 1896 y 1996 son bisiestos
// (son múltiplos de 4 y no de 100), pero 1800 y 1900 NO lo son (son múltiplos de 4 y de 100,
// pero no de 400); en cambio 2000 ES bisiesto (es múltiplo de 4, de 100 y de 400).
// d. es_MásAntiguaQue_, que dadas dos fechas indique si la primera es
// anterior a la segunda. AYUDA: usar funciones auxiliares mismoAño_Y_ y
// mismoMes_Y_, que reciben dos fechas e indican la condición
// correspondiente, y está_AntesQue_, que recibe dos meses e indica si el
// primero aparece antes que el segundo en el mismo año.
// e. primerDíaDelInviernoDe_, que describa el primer día del invierno del
// año dado (en el hemisferio sur).
// f. últimoDíaDelInviernoDe_, que describa el último día del invierno del
// año dado (en el hemisferio sur).
// g. está_EnInvierno, que dada una fecha indica si el día dado es uno del
// invierno. AYUDA: considerar utilizar las funciones de los 3 puntos anteriores.
// h. está_EnVerano, está_EnPrimavera y está_EnOtoño, siguiendo la
// misma estrategia que la utilizada para está_EnInvierno.
// i. estaciónDe_, que dada una fecha retorna la estación del año en la que
// ocurre la fecha. Para ello, definir un tipo variante Estación.
// j. añosTranscurridosEntre_Y_, que dadas dos fechas describa el número
// de años que hay entre las dos fechas (no tiene en cuenta períodos de menos
// de 1 año -- o sea, si la distancia es menor a 1 año, describe 0).
// k. siguienteFechaA_, que dada una fecha, devuelve la fecha del día
// siguiente. AYUDA: usar funciones auxiliares díaEnMesSiguienteAl_Del_
// y siguienteMesA_
// previaFechaA_, que dada una fecha, devuelve la fecha del día anterior.
// AYUDA: usar funciones auxiliares díaEnMesPrevioAl_Del_ y
// previoMesA_.