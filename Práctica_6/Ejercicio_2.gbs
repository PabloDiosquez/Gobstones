// Volver a definir el procedimiento SacarTodasLasDeColor_(colorASacar), que
// quita todas las bolitas del color dado por el parámetro color de la celda actual,
// pero esta vez SIN utilizar la expresión primitiva nroBolitas.

procedure SacarTodasLasDeColor_(colorASacar) {
    /*
        PROPÓSITO:
            * Quita todas las bolitas de color **colorASacar** de la celda actual.
        PARÁMETROS:
            * colorASacar: Color - Describe el color de las bolitas a sacar.
        PRECONDICIONES:
            * No tiene.
    */
    while (hayBolitas(colorASacar)) {
        Sacar(colorASacar)
    }
}