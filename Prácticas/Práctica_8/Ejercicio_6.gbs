// -El valor de mes sólo puede ir del 1 al 12.
//         -Si mes es igual a 4, 6, 9 u 11, los valores para día 
//         pueden encontrarse únicamente entre 1 y 30.
//         -Si mes es igual a 1, 3, 5, 7, 8, 10 o 12, los valores para 
//         día pueden encontrarse únicamente entre 1 y 31.
//          Si mes es igual a 2, los valores para día pueden encontrarse 
//          únicamente entre 1 y 29 sólo si año es divisible por 4. Sino
//          los valores para día pueden encontrarse únicamente entre 1 y 28.


// Declarar los tipos necesarios para representar fechas (según el calendario
// gregoriano utilizado para identificar nuestras fechas).
// AYUDA: declarar un tipo variante llamado Mes y un tipo registro llamado Fecha.

type Fecha is record {
    /*
        PROPÓSITO:
            * Modela una fecha según el calendario gregoriano.
    */

    field día // Número
    field mes // Mes
    field año // Número
}



type Mes is variant {
    /*
        PROPÓSITO:
            * Modela un mes del año.
    */

    case Enero      {}
    case Febrero    {}
    case Marzo      {}
    case Abril      {}
    case Mayo       {}
    case Junio      {}
    case Julio      {}
    case Agosto     {}
    case Septiembre {}
    case Octubre    {}
    case Noviembre  {}
    case Diciembre  {}
}